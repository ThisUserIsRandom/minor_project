-- show databases;
-- create database hobbists;
use hobbists;
-- show tables;

-- CREATE TABLE users (
--     id INT PRIMARY KEY AUTO_INCREMENT,     -- Unique identifier for each user
--     username VARCHAR(50) NOT NULL,         -- Username column with a maximum length of 50 characters
--     password VARCHAR(255) NOT NULL,        -- Password column (hashed), length can vary
--     email VARCHAR(100) NOT NULL,           -- Email column, max length of 100 characters
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, -- Date and time the record was created
--     api_key VARCHAR(100)
-- );